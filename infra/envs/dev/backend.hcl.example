# Partial backend config — copy to backend.hcl and fill in bootstrap outputs.
bucket         = "REPLACE_WITH_STATE_BUCKET"
key            = "envs/dev/terraform.tfstate"
region         = "ap-south-1"
dynamodb_table = "smartretailx-terraform-locks"
encrypt        = true
