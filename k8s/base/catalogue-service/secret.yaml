# TEMPLATE ONLY — do not commit real secrets.
# Replace placeholders or inject via External Secrets / AWS Secrets Manager CSI.
apiVersion: v1
kind: Secret
metadata:
  name: catalogue-service-secrets
  labels:
    app: catalogue-service
    app.kubernetes.io/name: catalogue-service
    app.kubernetes.io/part-of: smartretailx
type: Opaque
stringData:
  # Reserved for future catalogue credentials (e.g. DB URL when persisted)
  CATALOGUE_API_KEY: "<REPLACE_WITH_CATALOGUE_API_KEY_OR_LEAVE_EMPTY>"
